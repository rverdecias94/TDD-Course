import { PasswordChecker } from "../../app/pass_checker/PasswordChecker"

describe("password checker tet suite", () => {
  let sut: PasswordChecker;

  beforeEach(() => {
    sut = new PasswordChecker();
  }
  );
})